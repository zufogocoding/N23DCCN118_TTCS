services:
 backend:
    build: .
    ports:
     - '9000:9000'
    depends_on:
     - db
 db:
    image: postgres:latest
    environment:
     - POSTGRES_USER = admin
     - POSTGRES_PASSWORD = trongzufo62 
     - POSTGRES_DB = soundclown
    volumes:
      - postgres_data:/var/lib/postgresql/data:Z

volumes:
  postgres_data: 
      
